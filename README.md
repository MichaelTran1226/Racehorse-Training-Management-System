# EquiFlow

Local authentication demo: see [docs/auth-localhost.md](docs/auth-localhost.md) for the five actor accounts, shared password `equi123`, OTP `123456`, and run commands. Google OAuth is intentionally deferred.
