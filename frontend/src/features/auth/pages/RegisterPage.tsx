import React, { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { AuthShell } from './LoginPage';
export const RegisterPage:React.FC=()=>{const navigate=useNavigate();const [form,setForm]=useState({email:'',fullName:'',role:'HORSE_OWNER'});const [error,setError]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();if(!form.email.endsWith('@gmail.com')){setError('Vui lòng dùng địa chỉ Gmail cho bản demo.');return;}localStorage.setItem('equiflow:pending',JSON.stringify({...form, role:'HORSE_OWNER'}));navigate('/verify-otp');};return <AuthShell title="Tạo tài khoản Gmail" subtitle="Đăng ký local · chưa kết nối Google OAuth" footer={<span>Đã có tài khoản? <Link to="/login">Đăng nhập</Link></span>}><form onSubmit={submit} className="auth-form"><label>Họ và tên<input value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} required/></label><label>Gmail<input type="email" placeholder="tenban@gmail.com" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><p className="demo-hint">Registration is for Horse Owners only. Staff accounts are managed by the Club Manager.</p>{error&&<p className="form-error">{error}</p>}<button type="submit">Gửi mã OTP</button><p className="demo-hint">Mã OTP local mặc định: <strong>123456</strong> · hiệu lực 15 phút.</p></form></AuthShell>};
